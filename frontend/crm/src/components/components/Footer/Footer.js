import React from 'react'
import {Segment} from "semantic-ui-react";
import style from './Footer.module.css';

const Footer = (props) =>{
    return(
        <Segment inverted vertical className={style.footer}>
        </Segment>
    );
};

export default Footer;